Use the installed `standterm-external-agent` skill to operate the current StandTerm terminal. Resolve the live instance URL-first, run `hello`, and begin with read-only text or screenshot observation; if the current shell, TUI, login, log-stream, or other state remains uncertain, ask the user before sending input.
